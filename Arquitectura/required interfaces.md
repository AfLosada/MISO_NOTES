It is an [[interface]] that the component uses to do its job. It is technically a class behind the scenes, but to facilitate decoupling it interacts with the class that uses the [[provided interfaces]].

